
import Layout from "@/components/Layout";
import MintSection from "@/components/MintSection";

const Mint = () => {
  return (
    <Layout>
      <MintSection />
    </Layout>
  );
};

export default Mint;
